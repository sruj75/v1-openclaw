{
  "name": "v1-openclaw",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.11.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "openclaw:apply": "npm run build && node --no-warnings dist/openclaw/apply.js",
    "test": "npm run build && node --no-warnings --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
